# Sharqi Backend

Production-ready Node.js backend using Express, dotenv, and a scalable folder structure.

## Features

- Express.js server
- Environment variables with dotenv
- JSON parsing
- Error handling middleware
- Test route: `GET /api/health`
- Firebase Admin SDK preparation

## Installation

```bash
npm install